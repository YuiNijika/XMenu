#pragma once
#include <string>

namespace I18nData {
    // Embedded i18n JSON data
    // These are fallbacks if resource loading fails
    
    extern const char* GetZhJson();
    extern const char* GetEnJson();
    extern const char* GetJpJson();
    extern const char* GetRuJson();
}
